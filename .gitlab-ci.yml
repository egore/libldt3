variables:
  MAVEN_OPTS: >-
    -Dhttps.protocols=TLSv1.2
    -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository
    -Dorg.slf4j.simpleLogger.showDateTime=true
    -Djava.awt.headless=true

  MAVEN_CLI_OPTS: >-
    --batch-mode
    --errors
    --fail-at-end
    --show-version
    --no-transfer-progress
    -DinstallAtEnd=true
    -DdeployAtEnd=true

  SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"
  GIT_DEPTH: "0"
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_SUBMODULE_FORCE_HTTPS: "true"

image: maven:3-eclipse-temurin-21

include:
  - project: "appframework/gitlab-pipelines"
    ref: "main"
    file: "/steps/maven-compile.yml"

stages:
  - build
  - sast
  - transpile

cache:
  key: "${CI_COMMIT_REF_SLUG}"
  paths:
    - .m2/repository/
    - target/
    - .sonar/cache

compile:
  stage: build
  extends:
    - .step-maven-compile

sonarcloud-check:
  stage: sast
  needs:
    - compile
  script:
    - mvn sonar:sonar -Dsonar.projectKey=${CI_PROJECT_ROOT_NAMESPACE}_${CI_PROJECT_NAME} -Dsonar.organization=${CI_PROJECT_ROOT_NAMESPACE}
  only:
    - merge_requests
    - main
    - master

transpile:
  stage: transpile
  needs:
    - compile
  script: java -jar transpiler/target/libldt3-transpiler-*.jar
